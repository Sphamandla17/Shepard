<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white">

    <!-- TOP BAR -->
    <LinearLayout
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/white">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:background="@android:color/transparent"
            android:contentDescription="@string/back"
            android:padding="12dp"
            android:src="@drawable/ic_back" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/prayer_requests"
            android:textColor="@color/black"
            android:textSize="@dimen/text_size_h2"
            android:textStyle="bold" />

        <View
            android:layout_width="56dp"
            android:layout_height="1dp" />

    </LinearLayout>

    <!-- DESCRIPTION -->
    <TextView
        android:id="@+id/tvPrayerRequestsDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/spacing_large"
        android:layout_marginTop="@dimen/spacing_medium"
        android:layout_marginEnd="@dimen/spacing_large"
        android:text="@string/prayer_request_description"
        android:textColor="@color/grey_dark"
        android:textSize="@dimen/text_size_body" />

    <!-- SUBMIT PRAYER REQUEST -->
    <Button
        android:id="@+id/btnSubmitPrayer"
        android:layout_width="match_parent"
        android:layout_height="@dimen/button_height"
        android:layout_marginStart="@dimen/spacing_large"
        android:layout_marginTop="@dimen/spacing_large"
        android:layout_marginEnd="@dimen/spacing_large"
        android:background="@drawable/bg_button_primary"
        android:text="@string/submit_prayer_request"
        android:textAllCaps="false"
        android:textColor="@color/white" />

    <!-- MEMBER PRAYER REQUESTS -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvPrayerRequests"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="@dimen/spacing_medium"
        android:padding="@dimen/spacing_medium" />

</LinearLayout>